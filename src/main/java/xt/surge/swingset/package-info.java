/**
 * This is the base package for the entire Swingset library. This includes the render system and
 * all components. The project will call the engine rendering agent to start it, as well as
 * set the scenes and everything else.
 * 
 * @author Surge
 * @version 1.0
 */
package xt.surge.swingset;